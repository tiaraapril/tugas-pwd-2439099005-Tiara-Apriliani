# Sistem
```
- Halaman register peserta -> {
    email,
    nama,
    institusi,
    country,
    address
}

tabel registration

- Admin -> {
    username,
    password

    fungsinya: Mengelola data pendaftaran (edit dan delete). Delete dengan soft delete menambahkan field `is_deleted`
}
```